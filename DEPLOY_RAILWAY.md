# 🚂 Деплой бота на Railway

## Подготовка проекта

### ✅ Что уже готово:
- `requirements.txt` - все зависимости
- `Procfile` - команда запуска
- `runtime.txt` - версия Python
- `railway.json` - конфигурация Railway
- `.gitignore` - исключение ненужных файлов

### 📋 Необходимые переменные окружения:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
```

Опционально:
```env
DB_FILE=bot_database.db
```

---

## 🚀 Пошаговая инструкция

### Шаг 1: Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий (или создайте новый)

---

### Шаг 2: Загрузка кода

#### Вариант А: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub:
```bash
git init
git add .
git commit -m "Initial commit - Telegram Schedule Bot"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-repo.git
git push -u origin main
```

2. В Railway выберите этот репозиторий

#### Вариант Б: Через CLI

1. Установите Railway CLI:
```bash
npm install -g @railway/cli
```

2. Авторизуйтесь:
```bash
railway login
```

3. Создайте проект:
```bash
railway init
```

4. Деплой:
```bash
railway up
```

---

### Шаг 3: Настройка переменных окружения

В панели Railway:

1. Откройте ваш проект
2. Перейдите в **"Variables"**
3. Добавьте переменные:

```
TELEGRAM_BOT_TOKEN = 7494728506:AAG...
GOOGLE_API_KEY = AIzaSy...
GEMINI_API_KEY = AIzaSy...
```

**Как получить ключи:**

- **Telegram Bot Token**: [@BotFather](https://t.me/BotFather) → `/newbot`
- **Google API Key**: [Google Cloud Console](https://console.cloud.google.com) → APIs & Services → Credentials
- **Gemini API Key**: [Google AI Studio](https://aistudio.google.com/app/apikey)

---

### Шаг 4: Настройка базы данных

Railway автоматически создаст SQLite БД в файловой системе.

**⚠️ ВАЖНО**: На бесплатном тарифе Railway БД сбросится при каждом деплое!

**Решения:**

#### Вариант 1: PostgreSQL (рекомендуется для продакшна)
```bash
railway add --plugin postgresql
```

Потребуется адаптировать `database.py` для PostgreSQL.

#### Вариант 2: Persistent Volume (платная функция)
В настройках проекта добавьте Volume для `/app/bot_database.db`

#### Вариант 3: Оставить SQLite (для тестирования)
Подходит для тестирования, но данные будут теряться.

---

### Шаг 5: Проверка деплоя

1. В Railway откройте **"Deployments"**
2. Следите за логами сборки
3. Проверьте что бот запустился:
   - Должно быть: `🚀 Бот запущен и готов к работе!`
4. Напишите боту в Telegram: `/start`

---

## 📊 Мониторинг

### Логи в реальном времени:

**В веб-интерфейсе:**
Railway → Ваш проект → Logs

**Через CLI:**
```bash
railway logs
```

### Проверка статуса:
```bash
railway status
```

---

## 🔧 Обновление бота

### Через Git:
```bash
git add .
git commit -m "Update bot"
git push
```
Railway автоматически задеплоит изменения.

### Через CLI:
```bash
railway up
```

---

## 💰 Тарифы Railway

### Бесплатный план:
- **$5 бесплатно** каждый месяц (без карты)
- **$10 в месяц** при добавлении карты
- 500 часов выполнения
- 100 GB исходящего трафика
- ⚠️ Без Persistent Storage (данные сбрасываются)

### Платный план (от $5/мес):
- Persistent Volumes
- Больше ресурсов
- Приоритетная поддержка

---

## ⚠️ Важные замечания

1. **Логи**: Будут очищаться. Используйте внешний сервис если нужна история.

2. **Файлы**: Временные PDF и изображения создаются в `/app/temp_images/`
   - Будут удалены при перезапуске
   - Это нормально, бот их автоматически очищает

3. **База данных**: 
   - SQLite теряет данные при каждом деплое на бесплатном плане
   - Для продакшна используйте PostgreSQL

4. **Секреты**: Никогда не коммитьте `.env` файл!

5. **Ограничения API**:
   - Gemini: 15 запросов/минуту (бесплатно)
   - Google Drive: 100 запросов/100 секунд/пользователя
   - Telegram Bot API: 30 сообщений/секунду

---

## 🐛 Типичные проблемы

### Бот не запускается

**Проверьте:**
1. Переменные окружения установлены?
2. Token корректный?
3. Логи Railway - какая ошибка?

```bash
railway logs
```

### ModuleNotFoundError

Проверьте что `requirements.txt` содержит все зависимости:
```bash
pip freeze > requirements.txt
```

### API ключи не работают

1. Проверьте что они активны в соответствующих консолях
2. Проверьте квоты и лимиты
3. Для Google Drive API: включите API в Cloud Console

### База данных пустая

На бесплатном плане Railway это нормально после каждого деплоя.
Используйте PostgreSQL или платный план с Volumes.

---

## 📱 Использование в продакшне

### Рекомендации:

1. **БД**: Мигрируйте на PostgreSQL
   ```bash
   railway add --plugin postgresql
   ```

2. **Мониторинг**: Добавьте логирование в внешний сервис
   - Sentry для ошибок
   - LogDNA/Papertrail для логов

3. **Бэкапы**: Настройте автоматические бэкапы БД

4. **Домен**: Можно добавить свой домен (не обязательно для бота)

5. **Ресурсы**: Следите за использованием лимитов в Railway Dashboard

---

## 🔗 Полезные ссылки

- [Railway Docs](https://docs.railway.app)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [Railway Discord](https://discord.gg/railway)
- [Troubleshooting](https://docs.railway.app/troubleshoot/fixing-common-errors)

---

## ✅ Чеклист перед деплоем

- [ ] Все зависимости в `requirements.txt`
- [ ] `.gitignore` настроен правильно
- [ ] `.env` файл НЕ закоммичен
- [ ] Переменные окружения настроены в Railway
- [ ] Бот работает локально
- [ ] API ключи получены и активны
- [ ] Код залит в Git репозиторий (если деплой через GitHub)

---

## 🎉 Готово!

После успешного деплоя ваш бот будет работать 24/7 на серверах Railway!

Если возникли вопросы - проверьте логи или напишите в поддержку Railway.

